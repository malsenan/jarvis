[pytest]
testpaths = tests
pythonpath = .
# Slow tests load the Whisper model and run real transcription. They are
# excluded by default; run them with:  .venv/bin/pytest -m slow
addopts = -m "not slow"
markers =
    slow: loads large models / takes more than a few seconds
